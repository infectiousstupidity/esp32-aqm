#pragma once

// Local Wi-Fi credentials for the air-quality monitor.
//
// Setup: copy this file to secrets.h in the same directory and replace the
// placeholder values with your own network name and password:
//
//   cp include/secrets.example.h include/secrets.h
//
// secrets.h is listed in .gitignore and must never be committed.

#define WIFI_SSID "YOUR_WIFI_SSID"
#define WIFI_PASSWORD "YOUR_WIFI_PASSWORD"
